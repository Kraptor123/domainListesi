name: Domain Kontrol ve Güncelleme

on:
  schedule:
    - cron: '0/45 * * * *'
  workflow_dispatch:

env:
  FILE_PATH: eklenti_domainleri.txt
  TZ: Europe/Istanbul

jobs:
  domain-kontrolu:
    runs-on: ubuntu-latest

    steps:
      - name: Depoyu klonla
        uses: actions/checkout@v4

      - name: Python kurulumu
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Gerekli paketleri yükle
        run: |
          python -m pip install --upgrade pip
          pip install requests cloudscraper

      - name: Domainleri kontrol et ve güncelle
        run: |
          python - <<'EOF'
          import re
          from urllib.parse import urlparse
          from cloudscraper import CloudScraper

          scraper = CloudScraper()

          def get_final_url(url, timeout=10):
              """URL'nin son redirect edilen halini döndürür"""
              try:
                  url = url.strip().rstrip('/')
          
                  response = scraper.get(
                      url,
                      allow_redirects=True,
                      timeout=timeout,
                      headers={'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'}
                  )
          
                  final_url = response.url.rstrip('/')
                  parsed = urlparse(final_url)
                  clean_url = f"{parsed.scheme}://{parsed.netloc}"
          
                  return clean_url
          
              except Exception as e:
                  print(f"Hata ({url}): {str(e)}")
                  return None

          def parse_line(line):
              """Satırı parse eder: |Name:https://domain.com"""
              match = re.match(r'\|([^:]+):(https?://[^\s]+)', line.strip())
              if match:
                  name = match.group(1)
                  url = match.group(2).rstrip('/')
                  return name, url
              return None, None

          file_path = '${{ env.FILE_PATH }}'
          
          try:
              with open(file_path, 'r', encoding='utf-8') as f:
                  content = f.read()
          except FileNotFoundError:
              print(f"Dosya bulunamadı: {file_path}")
              exit(1)

          original_content = content
          changes_made = False

          print("Domain kontrolleri başlıyor...\n")

          lines = content.splitlines()
          
          for i, line in enumerate(lines):
              if not line.strip() or not line.strip().startswith('|'):
                  continue
          
              name, original_url = parse_line(line)
          
              if not name or not original_url:
                  continue
          
              print(f"Kontrol ediliyor: {name}")
              print(f"  Mevcut URL: {original_url}")
          
              final_url = get_final_url(original_url)
          
              if final_url and final_url != original_url:
                  print(f"  ✓ Yeni URL bulundu: {final_url}")
                  old_line = line
                  new_line = f"|{name}:{final_url}"
                  content = content.replace(old_line, new_line, 1)
                  changes_made = True
              else:
                  if final_url:
                      print(f"  → Değişiklik yok")
                  else:
                      print(f"  ✗ Erişilemedi, mevcut URL korunuyor")
          
              print()

          # Dosyayı sadece değişiklik varsa güncelle
          if changes_made and content != original_content:
              with open(file_path, 'w', encoding='utf-8') as f:
                  f.write(content)
              print("✓ Dosya güncellendi!")
          else:
              print("→ Güncelleme gerekmedi.")

          EOF

      - name: Değişiklikleri kaydet
        run: |
          git config user.name 'Domain Kontrol Botu'
          git config user.email 'bot@github-actions'
          
          git add ${{ env.FILE_PATH }}

          if git diff --staged --quiet; then
            echo "Değişiklik yok, commit atlanıyor"
            exit 0
          fi

          CURRENT_TIME=$(date +'%Y-%m-%d %H:%M')
          git commit -m "🔄 Domain güncellemesi - $CURRENT_TIME"
          git push